spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST_SQL:localhost}:5432/${DB_NAME:workdb}
    username: postgres
    password: ${DB_PASSWORD:Shaad#98}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  
  mongodb:
    uri: mongodb://${DB_HOST_NOSQL:localhost}:27017/${DB_NAME:workdb}

